export com.novel.po.news.NewsInfo
    -> package com.novel.user.dto.news

NewsInfoView {
    id,
    categoryId,
    categoryName,
    sourceName,
    title,
    updateTime,
    flat(newsContent) {
        content
    }
}

LaetstNewsView {
    id,
    categoryName,
    title,
    updateTime
}